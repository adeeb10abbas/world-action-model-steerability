# Copy this to the execution coordinator

Work in `adeeb10abbas/world-action-model-steerability`, branch `codex/nano-stock-workstation-20260926`. Read `docs/robolab-workshop-20260926/CLUSTER_EXECUTION_SPEC.md`, then `IMPLEMENTATION_PLAN.md`, `MODEL_CONFIGS.json`, and the illustrated prompt catalog. Use the latest pushed handoff commit and record its SHA.

Implement and run the new **native RoboLab** workshop study on the cluster: Cosmos3 Nano Policy DROID (N3), Cosmos3 Edge Policy DROID (E3), and root-BF16 FLUX 3 Action DROID (F3). The fixed question is: **Can a model's predicted future help distinguish following the wrong goal from failing to execute the right one?** Compare the same physical state and goal under D/S/I wording; goal changes are separate controls. Save contemporaneous predicted videos, proposed actions and aligned executed behavior.

Start with `python3 tools/build_robolab_execution_handoff.py --check`. The inventory contains 42 Nano development episodes, two Edge and two FLUX integration episodes, then 336 confirmation episodes per eligible model: at most 1,054 total. The 1,008 confirmation rows are planned, not yet bound to physical snapshots. Implement the state/scorer/recording contracts and qualify each model before releasing its lane. Do not interpret existing pilot success or the old scene registry as completed qualification of this new study.

**Do not restart the old SGW-01 campaign, redesign assets, clean backgrounds, move cameras, or run the old 1,566-cell queue.** Use the five existing native scenes, exact prompts and original cameras. Preserve the old records. Register eight modest pose variations per scene with the bounded checks in the spec; this is reset preparation, not a new scene project. Keep valid failures and all partial attempts.

Divide work across the runner/state implementation, N3, E3, F3, and annotation/analysis roles as resources permit. Nano/Edge must not wait for FLUX. E3 requires its JSON prompt and guidance-interval settings. Give FLUX same-sample video export four engineer-hours; if it cannot preserve actions and establish frame correspondence, report F3 blocked for confirmation and continue eligible models. No substitute video generation or unannounced action-only study.

Run a finite development batch, freeze the common release, and begin qualified confirmation lanes as soon as checks pass. No tuning sweeps, automatic request retries or repeating already-passed checks. Missing runtime configuration or human annotation must be reported with the exact artifact/action needed, while independent work continues. Use the Kubernetes, storage, failure and progress contracts in the spec. Report completed whole state blocks, counts by model, data locations, measured ETA and blockers. Finish with reproducible tables/figures and a results-grounded four-page paper draft; do not submit the paper.
